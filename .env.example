# Leave ADMIN_PASSWORD empty to run the admin unlocked on your own machine.
# Set it before putting this on the internet.
ADMIN_PASSWORD=
AUTH_SALT=change-me
# Optional: where the SQLite file lives (defaults to ./data/quotations.db)
# DB_PATH=/var/data/quotations.db
